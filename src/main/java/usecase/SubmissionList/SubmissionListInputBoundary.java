package usecase.SubmissionList;

public interface SubmissionListInputBoundary {
    /**
     * Execute function for the interactor.
     * @param data the input data.
     */
    void execute(SubmissionListInputData data);
}
